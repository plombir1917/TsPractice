const pairs: [string, string][] = [
  ['key', 'value'],
  ['key2', 'value2'],
];

const data: [number, boolean, string] = [1, true, 'lodash'];

//кортежи часто используются для csv - comma separated value, данные разделённые запятой

const doc: [string, number, boolean, Date][] = [];
doc.push(['string', 1, true, new Date(1985)]);
